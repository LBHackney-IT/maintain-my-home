= back_link

= simple_form_for @form, url: request.fullpath do |f|
  %fieldset
    %legend.question
      %h1= t('contact-details.title')

    .answers
      = f.input :full_name
      = f.input :telephone_number
      = f.button :submit
